spring.application.name=Backend
# Automatic Reload
spring.devtools.restart.enabled=true
spring.devtools.restart.additional-paths=static/**,templates/**
spring.thymeleaf.cache = false
spring.freemarker.cache = false
spring.groovy.template.cache = false

# ORM
# next line deletes the database on startup or shutdown
#spring.jpa.hibernate.ddl-auto=create
# next line updates the database on startup
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${AZURE_MYSQL_CONNECTIONSTRING:jdbc:mysql://swipeurstylebackend-server.mysql.database.azure.com:3306/swipeurstyleback-database?useSSL=true}
spring.datasource.username=nwvlcnlnlv
spring.datasource.password=UvFv$mjPi5etF3lE
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true


# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

spring.main.allow-circular-references=true
